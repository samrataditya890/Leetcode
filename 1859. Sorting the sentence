class Solution {
public:
    string sortSentence(string s) {
        vector<string> ans(10);
        string temp;
        int cnt=0, index=0;
        
        while(index<s.size()){
            if(s[index]==' '){
                int x=temp[temp.size()-1]-'0';
                temp.pop_back();
                ans[x]=temp;
                temp.clear();
                cnt++;
            }
            else{
                temp+=s[index];
            }
            index++;
        }
        int x=temp[temp.size()-1]-'0';
                temp.pop_back();
                ans[x]=temp;
                temp.clear();
                cnt++;
        for(int i=1;i<=cnt;i++){
            temp+=ans[i];
            temp+=' ';
        }
        temp.pop_back();
        return temp;
                
         
    }
};
